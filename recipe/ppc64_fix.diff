diff --git a/CMakeLists.txt b/CMakeLists.txt
index 65c0348a..d28b41b2 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -427,6 +427,13 @@ if(_HEYOKA_LLVM_STATIC_LINK AND HEYOKA_HIDE_LLVM_SYMBOLS)
     unset(CMAKE_REQUIRED_QUIET)
 endif()
 
+# Workaround for compilation failure on ppc64 for some combinations
+# of GCC and Boost versions:
+# https://github.com/boostorg/multiprecision/issues/419
+if(${CMAKE_SYSTEM_PROCESSOR} MATCHES "ppc64")
+    target_compile_definitions(heyoka PRIVATE NO_WARN_X86_INTRINSICS)
+endif()
+
 # Mandatory dependency on fmt.
 target_link_libraries(heyoka PUBLIC fmt::fmt)
 
